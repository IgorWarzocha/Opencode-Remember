{
  "name": "@howaboua/opencode-remember",
  "version": "0.1.0",
  "type": "module",
  "description": "Semantic long-term memory for OpenCode with local embeddings",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "keywords": ["opencode", "opencode-plugin", "plugin", "memory", "semantic-search", "embeddings", "long-term-memory"],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.0.0"
  },
  "dependencies": {
    "fastembed": "^1.14.1",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "@types/bun": "^1.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepublishOnly": "npm run build"
  }
}
